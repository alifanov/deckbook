#!/bin/sh
set -e
# Схему приложение не трогает: миграции накатывает отдельный сервис migrate,
# и приложение стартует только после того, как тот завершился успешно.
exec ./node_modules/.bin/next start --port "${PORT:-3000}"
